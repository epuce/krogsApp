<div class= "form" >
  <% if flash[:alert] %>
      <p id= "notice" > <%= flash[:alert] %> </p>
  <% end %>

  <%= form_tag('/login') do %>
      <fieldset>

        <legend> Lūdzu pierakstieties </legend>

        <div>
          <%= label_tag :name, 'Lietotājvārds:' %>
          <%= text_field_tag :name, params[:name] %>
        </div>

        <div>
          <%= label_tag :password, 'Parole:' %>
          <%= password_field_tag :password, params[:password] %>
        </div>

        <div>
          <%= submit_tag "Pierakstīties", class:'btn btn-s btn-submit' %>
        </div>
      </fieldset>
  <% end %>
</div>
